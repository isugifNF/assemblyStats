// need to verify that nova is the same as condo for modules

process {
  executor = 'slurm'
  clusterOptions =  '-N 1 -n 36 -t 48:00:00'

  withName:runBUSCO {
    clusterOptions =  '-N 1 -n 36 -t 168:00:00'
  }
}
